# Project Memory

更新时间：2026-08-07

## 1. 项目身份

本项目是“买赠礼品图更新 / 礼品中心后台原型”，主要用于管理后台原型迭代。

协作角色要求：以十年资深高级产品经理 / 产品负责人视角推进，兼顾业务目标、后台信息架构、运营落地、研发成本、测试边界和风险预判。

## 2. Git 仓库

管理后台原型仓库：

- GitHub：<https://github.com/moonqin800-sys/demomoon>
- 本地目录：`/Users/buyueqin/Downloads/买赠礼品图更新`
- 主分支：`main`

移动端手机号端原型仓库：

- GitHub：<https://github.com/moonqin800-sys/mobilemoon.git>

## 3. 固定协作规则

1. 每次更新管理后台原型前，先执行 `git pull` 拉取 `demomoon` 最新代码。
2. 每次更新移动端原型前，先执行 `git pull` 拉取 `mobilemoon` 最新代码。
3. 每次生成新的原型页面后，需要询问是否上传到 Git。
4. 后续新增功能基于当前最新原型继续迭代。
5. 保留历史版本文件，新需求生成新的版本号文件，并同步更新入口文件。
6. 只提交本次需求相关文件，不把无关 PPT、视频、临时文件夹一起提交。

## 4. 当前原型版本

当前后台入口文件：

- `gift-admin-prototype.html`

当前最新版本文件：

- `gift-admin-prototype-v2.15-20260809.html`

当前入口文件与 `v2.15` 内容一致。

## 5. 已完成的重要原型改动

礼品库：

- “新增礼品”改为“同步礼品”，强调礼品数据来自上游系统同步，不是后台手工新增。
- 去掉重置按钮。
- 去掉顶部统计板块：礼品总数、启用礼品数、待补素材、活动绑定。
- 列表去掉“复制”操作。
- 礼品库存 / 礼品库基础信息中，礼品 code、礼品名称不可修改。

活动礼品配置：

- 去掉“批量停用”。
- “添加礼品”改为“同步数据”。
- 同步数据前强调需要先选择活动。
- 同步数据弹框改为轻提示流程：同步中、同步完成，不展示具体添加绑定内容。
- 列表去掉排序、展示时间、状态字段。
- “覆盖配置”文案最终调整为“素材自定义”。
- 活动素材自定义支持修改产品别名。

会员服务轮播图管理：

- 左侧导航调整为“礼品中心”一级菜单，下挂“礼品库 / 活动礼品配置”两个二级菜单。
- 新增独立一级菜单“会员服务轮播图管理”。
- 支持礼品页轮播 Banner 的新增、编辑、排序、上下架、筛选、发布。
- 支持配置跳转类型：外链 H5、小程序页面、不跳转。
- 支持配置 H5 链接、小程序 AppID、小程序页面路径、生效时间和运营备注。
- 右侧消费者端预览支持展示“我的礼品页”轮播 Banner 效果。
- 页面内展示接口配置 JSON，方便研发评审字段结构。
- 去掉页面内水平导航，页面切换仅保留左侧导航。
- 会员服务轮播图管理页去掉页面位置、导出配置和顶部统计概览区块，页面首屏直接进入筛选、列表与编辑配置。

活动管理：

- 新增独立一级菜单“活动管理”，采用全宽后台台账布局。
- 活动管理支持按活动名称/代码和结束时间筛选，并保留导出 Excel 操作入口。
- 活动管理列表字段包括 ID、活动名称、活动代码、开始时间、结束时间、物流填写截止天数、物流填写结束时间、活动类型、活动品牌、活动简介和操作。
- 活动管理编辑弹框参考现有业务后台样式，补充活动基础信息、物流填写信息、工作时间、关键字、活动简介和操作手册区域。
- 活动管理编辑弹框新增必填下拉“注册隐私条款渠道”，可选择会员服务小程序、E-learning、iDream、妈妈班、路演。

质检报告管理：

- 新增独立一级菜单“质检报告管理”。
- 支持 PDF 批量上传、单个上传、下载、单条删除、批量删除和关键词/校验状态/文件类型查询。
- 文件名支持抽屉内修改，自动补全 `.pdf` 后缀并校验重名；批次不一致文件保留“_不一致”标记。
- 列表记录识别批次号、批次校验状态、操作人、创建时间和更新时间。
- 质检报告页面采用全宽文件台账布局，不展示额外的右侧说明面板。
- “操作记录”作为质检报告管理下的二级菜单，记录上传、识别校验、重命名、下载和删除等操作。
- 质检报告列表新增“查看”操作，支持在弹窗中预览对应 PDF 文件。

隐私条款管理：

- 新增独立一级菜单“隐私条款管理”。
- 隐私条款管理菜单位于质检报告管理菜单组下方。
- 支持按关键词、渠道、条款类型和发布状态筛选渠道隐私条款。
- 支持维护会员服务小程序、E-learning、iDream、妈妈班、路演等渠道的隐私政策、儿童个人信息处理规则、个人信息收集清单、个人信息共享清单和服务协议。
- 新增草稿、编辑条款均使用弹框维护，不在列表页常驻表单。
- 弹框内展示当前发布版本、本次发布版本号和条款表单，版本号只读，由系统按渠道+条款类型自动递增；弹框底部按钮水平居中。
- 点击列表或编辑弹框内的“发布”时，先展示二次确认弹框，确认后才生成新版本并发布。
- 隐私条款管理下新增二级菜单“隐私条款维护 / 隐私条款记录”。
- 隐私条款记录支持按 OpenID/会员ID、渠道、隐私条款类型、隐私政策版本、创建时间查询。
- 隐私条款记录列表字段包括 openID、会员ID、渠道、隐私条款类型、隐私政策版本、创建时间；只有隐私政策记录有版本号，其他条款类型显示为空或 `-`。
- 隐私条款管理页改为全宽后台台账，不展示右侧消费者端预览、顶部统计概览、底部规则说明和接口代码块。
- 新增/编辑隐私条款时，条款正文改为上传 PDF 文件，按渠道、条款类型和版本号关联保存。
- 隐私条款维护列表操作列新增“历史版本”按钮，点击后弹框展示渠道、条款类型、版本、创建时间。
- 隐私条款新增/编辑弹框已移除“老用户版本更新弹窗文案”维护项。
- 后台入口默认选中“隐私条款管理 / 隐私条款维护”，无 hash 或 nav 参数时默认进入隐私条款维护页。
- 隐私条款新增/编辑时，仅“隐私政策”类型展示当前版本和本次发布版本号；其他条款类型不展示版本号，发布时不自动递增版本。
- 隐私政策版本号采用 `V主版本.小版本` 格式，例如 `V1.0`、`V1.1`；发布时自动递增小版本号。

## 6. 版本记录

- `v1.0-20260722`：礼品中心后台原型初始版本。
- `v1.1-20260722`：方案文档与原型早期迭代。
- `v1.2-20260722`：后台原型继续补充。
- `v1.3-20260722`：礼品中心配置能力迭代。
- `v1.4-20260722`：精简礼品库和活动礼品配置，同步改为轻提示流程。
- `v1.5-20260723`：礼品库去掉复制，礼品 code 和礼品名称改为只读。
- `v1.6-20260724`：新增会员服务轮播图管理一级菜单，集成礼品页 Banner 上传、排序、跳转配置、上下架和小程序预览，并调整左侧导航层级。
- `v1.7-20260729`：新增质检报告管理一级菜单，补充 PDF 文件上传、查询、下载、删除、重命名、批次校验和操作字段展示。
- `v1.8-20260729`：移除质检报告右侧说明面板，将“操作记录”调整为质检报告管理下的二级菜单，并补充报告文件操作日志内容。
- `v1.9-20260729`：将礼品中心调整为一级菜单样式，质检报告列表新增 PDF 查看预览入口。
- `v2.4-20260804`：新增隐私条款管理后台原型，补充渠道隐私条款内容维护、自动版本号、发布历史、用户同意记录字段和前端弹窗预览。
- `v2.5-20260805`：按反馈调整隐私条款管理，移动菜单位置，去掉统计/说明/代码/右侧预览，新增条款改为弹框维护。
- `v2.6-20260805`：新增隐私条款草稿弹框去掉右侧版本历史内容，底部操作按钮改为水平居中。
- `v2.7-20260805`：隐私条款发布新增二次确认弹框，确认后再生成新版本并发布。
- `v2.8-20260805`：新增二级菜单“隐私条款记录”，补充查询区和 openID、会员ID、渠道、条款类型、版本、创建时间列表。
- `v2.9-20260806`：隐私条款正文改为 PDF 文件上传维护，老用户版本更新弹窗文案改为富文本编辑器。
- `v2.10-20260806`：隐私条款维护操作列新增“历史版本”按钮，并以弹框展示渠道、条款类型、版本和创建时间。
- `v2.11-20260807`：新增一级菜单“活动管理”和活动编辑弹框，新增必填“注册隐私条款渠道”下拉，并移除隐私条款弹框中的老用户版本更新弹窗文案。
- `v2.12-20260807`：调整后台入口默认选中“隐私条款维护”，同步默认面包屑、左侧菜单高亮和全宽台账布局。
- `v2.13-20260807`：调整隐私条款新增/编辑与发布规则，仅“隐私政策”类型维护版本号，其他条款类型列表、弹框、历史版本和发布确认均不展示或递增版本号。
- `v2.14-20260809`：调整隐私条款记录页，版本筛选和列表字段改为“隐私政策版本”，仅隐私政策记录展示版本号，其他条款类型展示为空或 `-`。
- `v2.15-20260809`：调整隐私政策版本号格式为 `V1.0`、`V1.1` 形式，发布递增规则改为小版本递增，并同步示例数据、PDF 文件名和同意记录版本。

## 7. 后续迭代建议

后续新增需求优先围绕这几条主线处理：

1. 上游同步：明确同步字段、同步状态、失败提示、差异更新策略。
2. 礼品素材：主图、活动展示图、详情图、物料图的尺寸规范和校验。
3. 活动配置：活动维度只做绑定、启停、展示策略和素材自定义。
4. 权限风控：礼品启停、素材自定义、活动绑定建议记录操作日志。
5. 验收体验：增加消费者端预览，降低运营配置错误。

## 8. 当前本地未入库文件

以下文件目前按无关素材处理，默认不提交：

- `未命名文件夹 3/`
- `赠品用图优化方案20260722.pptx`
