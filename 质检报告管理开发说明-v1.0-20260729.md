# 质检报告管理开发说明

## 1. 项目背景

为支持质检 PDF 文件的后台统一管理，本次在管理后台新增「质检报告管理」一级菜单，并在其下新增「操作记录」二级菜单。后台需支持质检报告 PDF 的上传、查询、预览、下载、删除、文件名修改及操作追溯，降低人工维护文件台账和批次核对的成本。

原型地址：
https://moonqin800-sys.github.io/demomoon/gift-admin-prototype.html?fresh=00894a5

## 2. 功能范围

本期建设范围包含：

- 新增一级菜单：质检报告管理
- 新增二级菜单：操作记录
- 质检报告列表查询
- PDF 文件批量上传、单个上传
- PDF 文件查看预览
- PDF 文件下载
- PDF 文件删除
- PDF 文件名修改
- 文件基础信息记录：文件名、识别批次号、文件大小、文件类型、操作人、创建时间、更新时间
- 操作记录追踪：上传、修改文件名、查看、下载、删除等动作

本期原型已去掉：

- 校验状态筛选
- 表格校验状态列
- 页面底部质检规则说明

## 3. 菜单结构

后台左侧导航结构如下：

| 菜单层级 | 菜单名称 | 页面说明 |
|---|---|---|
| 一级菜单 | 礼品中心 | 保持现有礼品库、活动礼品配置能力 |
| 一级菜单 | 会员服务轮播图管理 | 保持现有 Banner 配置能力 |
| 一级菜单 | 质检报告管理 | 管理 PDF 质检报告文件 |
| 二级菜单 | 操作记录 | 查看质检报告相关操作日志 |

进入「质检报告管理」及「操作记录」时，右侧消费者端预览区域隐藏，页面聚焦后台管理能力。

## 4. 质检报告列表

### 4.1 查询条件

| 字段 | 类型 | 说明 |
|---|---|---|
| 关键词 | 输入框 | 支持按文件名、批次号、报告编号查询 |

### 4.2 统计信息

| 指标 | 说明 |
|---|---|
| 文件总数 | 当前系统内质检 PDF 文件总数量 |
| 最近更新 | 最近一次文件更新时间 |

### 4.3 列表字段

| 字段 | 说明 |
|---|---|
| 选择 | 支持单选、全选，用于批量下载和批量删除 |
| 文件名 | PDF 文件展示名称 |
| 识别批次号 | 系统从 PDF 内容中识别出的批次号 |
| 文件大小 | PDF 文件大小 |
| 操作人 | 最近一次操作该文件的用户 |
| 创建时间 | 文件首次上传或创建时间 |
| 更新时间 | 文件最近更新时间 |
| 操作 | 查看、修改、下载、删除 |

## 5. 上传功能

### 5.1 单个上传

用户点击「单个上传」后选择一个 PDF 文件。系统完成文件上传、文件信息入库，并在列表中展示。

基础规则：

- 仅支持 PDF 文件
- 文件名不可重复
- 上传成功后记录操作人、创建时间、更新时间
- 上传完成后写入操作记录

### 5.2 批量上传

用户点击「批量上传」后可一次选择多个 PDF 文件。系统逐个处理文件，并返回上传结果。

建议后端返回：

| 字段 | 说明 |
|---|---|
| successCount | 上传成功数量 |
| failCount | 上传失败数量 |
| failList | 失败文件清单及原因 |

## 6. PDF 查看预览

用户点击列表操作「查看」后，打开 PDF 预览弹窗。

预览弹窗展示：

- PDF 文件名
- 文件类型
- 识别批次号
- 最近操作人
- PDF 文件内容 iframe 预览

技术建议：

- 后端返回可访问的 PDF 文件 URL
- URL 建议使用带权限校验的临时访问地址
- 预览地址需避免直接暴露永久私有文件路径

## 7. 文件名修改

用户点击「修改」后，打开右侧抽屉。

抽屉字段：

| 字段 | 说明 |
|---|---|
| 当前文件名 | 只读 |
| 新文件名 | 必填 |
| 识别批次号 | 只读 |
| 最近操作人 | 只读 |

规则：

- 新文件名不能为空
- 如未输入 `.pdf` 后缀，系统自动补全
- 新文件名不可与已有文件名重复
- 修改成功后更新操作人、更新时间
- 修改成功后写入操作记录

## 8. 下载与删除

### 8.1 下载

支持单个文件下载和批量下载。

- 单个下载：点击列表行内「下载」
- 批量下载：勾选多个文件后点击「下载所选」

批量下载建议后端打包为 zip 文件返回。

### 8.2 删除

支持单个文件删除和批量删除。

- 删除前建议增加二次确认
- 删除成功后更新列表和统计数据
- 删除动作必须写入操作记录
- 如业务要求可追溯，建议采用逻辑删除

## 9. 操作记录

操作记录作为「质检报告管理」下的二级菜单。

### 9.1 查询条件

| 字段 | 类型 | 说明 |
|---|---|---|
| 操作对象 | 输入框 | 支持按文件名、文件编号查询 |
| 操作类型 | 下拉框 | 全部类型、上传文件、识别校验、修改文件名、下载文件、删除文件 |
| 操作人 | 下拉框 | 全部人员、活动运营、管理员、系统 |

### 9.2 记录字段

| 字段 | 说明 |
|---|---|
| 操作时间 | 动作发生时间 |
| 操作内容 | 操作描述 |
| 操作人 | 执行动作的用户或系统 |
| 文件编号 | 关联质检报告文件 ID |
| 文件名 | 操作对象文件名 |
| 操作类型 | 上传、修改、下载、删除、查看等 |

## 10. 建议数据模型

### 10.1 质检报告表 quality_report_file

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint/string | 主键 |
| file_name | varchar | 文件名 |
| file_url | varchar | 文件存储地址 |
| report_type | varchar | 检测报告、检测结果 |
| detected_batch_no | varchar | 识别批次号 |
| file_size | bigint | 文件大小，单位 byte |
| operator_id | varchar | 最近操作人 ID |
| operator_name | varchar | 最近操作人名称 |
| created_at | datetime | 创建时间 |
| updated_at | datetime | 更新时间 |
| deleted | tinyint | 是否删除 |

### 10.2 操作记录表 quality_report_operation_log

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint/string | 主键 |
| report_id | bigint/string | 质检报告 ID |
| file_name | varchar | 操作时文件名 |
| operation_type | varchar | 操作类型 |
| operation_content | text | 操作内容 |
| operator_id | varchar | 操作人 ID |
| operator_name | varchar | 操作人名称 |
| created_at | datetime | 操作时间 |
| client_ip | varchar | 操作 IP，可选 |

## 11. 建议接口

| 接口 | 方法 | 说明 |
|---|---|---|
| /api/quality-reports | GET | 查询质检报告列表 |
| /api/quality-reports/upload | POST | 单个或批量上传 PDF |
| /api/quality-reports/{id} | GET | 获取文件详情 |
| /api/quality-reports/{id}/preview-url | GET | 获取 PDF 预览地址 |
| /api/quality-reports/{id}/download | GET | 下载单个 PDF |
| /api/quality-reports/batch-download | POST | 批量下载 PDF |
| /api/quality-reports/{id}/rename | PUT | 修改文件名 |
| /api/quality-reports/{id} | DELETE | 删除单个 PDF |
| /api/quality-reports/batch-delete | POST | 批量删除 PDF |
| /api/quality-report-logs | GET | 查询操作记录 |

## 12. 权限建议

| 权限点 | 说明 |
|---|---|
| quality_report:view | 查看列表、详情、预览 |
| quality_report:upload | 上传 PDF |
| quality_report:download | 下载 PDF |
| quality_report:update | 修改文件名 |
| quality_report:delete | 删除 PDF |
| quality_report:log:view | 查看操作记录 |

## 13. 验收标准

- 左侧菜单存在「质检报告管理」一级菜单和「操作记录」二级菜单
- 质检报告列表可按关键词查询
- 列表不展示「校验状态」字段
- 页面底部不展示「质检规则」说明
- 支持单个 PDF 上传
- 支持多个 PDF 批量上传
- 支持 PDF 在线预览
- 支持单个和批量下载
- 支持单个和批量删除
- 支持文件名修改，且文件名不可重复
- 所有上传、修改、下载、删除动作均写入操作记录
- 列表统计数据随上传、删除、修改动作实时更新

## 14. 测试重点

- 非 PDF 文件上传拦截
- 重复文件名上传拦截
- 大文件上传稳定性
- 批量上传部分成功、部分失败场景
- 文件名为空、重复、无 `.pdf` 后缀场景
- PDF 预览地址失效或无权限场景
- 批量下载文件数量较多场景
- 删除后的列表刷新和操作记录写入
- 不同角色权限按钮展示和接口鉴权

